
defmodule Pong.Sprite do
  
  # game size could be 4000x4000, make it so that the x,y 
  # fit inside the 4k x 4k
  # But the goal is to rotate the field.
  # Math.
  def camera_view(width, height, x, y) do
    {x, y}
  end

end
